spring.application.name=not-another-rewatch

# DynamoDB - LocalStack for local dev
aws.dynamodb.endpoint=http://localhost:4566
aws.region=us-west-2

# Semantic search
app.embeddings.path=../data/embeddings.json
app.embedding-server.url=http://localhost:8081
